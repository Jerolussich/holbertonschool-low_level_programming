#include "dog.h"
#include <stdlib.h>

/**
 * free_dog - free malloc
 * @d: pointer
 * Return: a null
 */

void free_dog(dog_t *d)
{

	if (d == NULL)
	{
		return (NULL);

		if (new_d->name == NULL)
		{
			free(d);
			return (NULL);
		}
		if (new_d->owner == NULL)
		{
			free(new_d->name);
			free(new_d);
			return (NULL);
		}
	}
}
