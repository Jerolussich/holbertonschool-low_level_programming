#include "main.h"
/**
 * _isdigit - check digit or otherwise
 * @c: variable that stores value
 * Description: check digit or nondigit
 * Return: 1 digit, 0 othercase
 **/

int _isdigit(int c)
{
	if (c >= 48 && 57 >= c)

		return (1);


	else
		return (0);
}
