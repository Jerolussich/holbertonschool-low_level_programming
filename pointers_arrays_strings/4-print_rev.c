#include "main.h"

/**
 * _puts - print char
 * @str: stores a value
 * Return: returns sums of string
 **/

void print_rev(char *s)
{

	int r = 0;

	while (*str != '\0')
	{
		str++;
		r++;
	}
	while (r > 0)
	{
	_putchar(*str);
	r--;
	}

_putchar('\n');

}
