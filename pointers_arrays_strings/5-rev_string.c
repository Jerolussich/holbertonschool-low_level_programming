#include "main.h"

/**
 * rev_string - print char in reverse
 * @s: stores a value
 * Return: returns sums of string
 **/

void rev_string(char *s)
{

	int add_index = 0;
	char array[r];
	int r = 0;

	while (*s != '\0')
	{
		s++;
		r++;
	}
		s--;

	while (r > 0)
	{
		_putchar(*s);
		add arry 
		s--;
		r--;
	}

	_putchar('\n');
}
