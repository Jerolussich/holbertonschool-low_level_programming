#include "main.h"
#include <stdio.h>
/**
 * rev_string - reverse a string
 * @s: pointer
 * Return: 0
 **/


void rev_string(char *s)
{
	int i = 0;
	int n = 0;
	int *dest = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	for (; i != 0; i--)

	{

		dest[n] = s[i];
		n++;
	}
}
