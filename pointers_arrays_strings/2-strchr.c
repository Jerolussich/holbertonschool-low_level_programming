#include "main.h"
#include <stdio.h>
/**
 * _strchr - fill memory with constante byte
 * @dest: pointer
 * @src: pointer
 * @c: a character
 * Return: dest
 **/

char *_strchr(char *s, char c)
{
	unsigned int i = 0;

	for (;s != c ; s++)
	{
	}

	return (s);
}
