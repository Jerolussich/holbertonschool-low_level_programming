#include "main.h"
#include <stdio.h>
/**
 * _strchr - fill memory with constante byte
 * @c: a character
 * @s: pointer
 * Return: dest
 **/

char *_strchr(char *s, char c)
{
	unsigned int i = 0;

	for (;s[i] != c && s[i] '\0'; s++)
	{
	}

	return (s);
}
