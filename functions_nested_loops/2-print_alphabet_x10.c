#include "main.h"

/**
 * print_alphabet_x10 - check the code.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)

{

	char ch;
	int t;

	for (t = "0"; t <= "10"; t++)

	{

	if (ch = 'a'; ch <= 'z'; ch++)

		_putchar(ch);
	}

		_putchar('\n');

	return (0);
}
